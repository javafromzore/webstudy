########################################配置中心配置########################################
spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        serverAddr: localhost:8848
        fileExtension: yaml
        namespace: dev